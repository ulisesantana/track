import {Configuration} from "../../src/core";

export const configuration: Configuration = {
    apiToken: 'fake-api-token',
    defaultTimeEntry: 'Working',
    projectId: 420,
    workspaceId: 100
}
